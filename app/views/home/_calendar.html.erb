<div class="row">
  <div class="col-md-12">
    <div class="panel panel-default">
      <div class="panel-heading">
        <h3 class="panel-title text-center"><%= "#{todays_date.strftime("%B")} #{todays_date.year}" %></h3>
      </div>
      <div class="panel-body">
        <%= calendar todays_date do |date| %>
          <%= date.day %>
          <% if @dated_events[date] %>
            <ul>
              <% @dated_events[date].each do |event| %>
                <li> <%= event.name %> </li>
              <% end %>
            </ul>
          <% end %>
        <% end %>
      </div>
    </div>
  </div>
</div>

